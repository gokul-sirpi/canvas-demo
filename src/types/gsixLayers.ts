export type GsixLayer = {
  layerType: 'GsixLayer'
  layerName: string;
  layerId: string;
  gsixLayerId: string;
  visible: boolean;
  selected: boolean;
  isCompleted: boolean;
  layerColor: string;
};
